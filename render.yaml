services:
  - type: web
    name: landing-page
    runtime: static
    buildCommand: npm ci && npm run build -- --configuration production
    staticPublishPath: dist/landing-page/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
