// Template for src/environments/environment.ts and environment.development.ts.
// Copy this file to those two names and fill in your real Apps Script /exec
// URLs. Both filenames are gitignored — they never get committed.
//
// On Render, the real values come from environment variables (see
// render.yaml) and are written into environment.ts at build time by
// scripts/generate-env.js.
export const environment = {
  experienceApiUrl: 'https://script.google.com/macros/s/REPLACE_ME/exec',
  careersApiUrl: 'https://script.google.com/macros/s/REPLACE_ME/exec',
  offersApiUrl: 'https://script.google.com/macros/s/REPLACE_ME/exec',
  developerProfilesApiUrl: 'https://script.google.com/macros/s/REPLACE_ME/exec',
  leadMagnetApiUrl: 'https://script.google.com/macros/s/REPLACE_ME/exec',
};
